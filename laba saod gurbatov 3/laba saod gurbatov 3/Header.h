#pragma once
namespace logic {
    unsigned int cycle(unsigned int a, unsigned int b);                   // цикл
    unsigned int recurs(unsigned int a, unsigned int b);                   // рекурсия
    void f_out(unsigned int a, unsigned int b);                                                // вывод в файл
}
